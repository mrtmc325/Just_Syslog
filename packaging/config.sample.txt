# Syslog Collector configuration (macOS / Linux)
# Edit, then restart the service:
#   Linux:  sudo systemctl restart syslog-collector
#   macOS:  sudo launchctl kickstart -k system/house.conner.syslog-collector
log_dir=/var/log/syslog-collector
udp_port=514
ui_port=8514
max_file_mb=100
retention_days=30
max_total_mb=2048
